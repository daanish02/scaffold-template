---
import SelfCheck, {
  type SelfCheckQuestion,
} from "@daanish02/scaffold-core/components/interactive/SelfCheck.tsx";

export interface Props {
  questions: SelfCheckQuestion[];
}
const { questions } = Astro.props;
---

<SelfCheck questions={questions} client:visible />
